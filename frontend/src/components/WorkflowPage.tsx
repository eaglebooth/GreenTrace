import{PageHead}from"./PageHead";import{ActionForm,type Mode}from"./ActionForm";export function WorkflowPage({mode,eyebrow,title,copy,steps,reportId}:{mode:Mode;eyebrow:string;title:string;copy:string;steps:string[];reportId?:string}){return <><PageHead eyebrow={eyebrow} title={title} copy={copy}/><section className="section"><div className="work-grid"><aside className="guide"><h3>Before signing</h3><ol>{steps.map(s=><li key={s}>{s}</li>)}</ol></aside><ActionForm mode={mode} reportId={reportId}/></div></section></>}
