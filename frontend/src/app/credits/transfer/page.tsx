import{WorkflowPage}from"@/components/WorkflowPage";export default function P(){return <WorkflowPage mode="transfer" eyebrow="Function 08 / transfer_credits" title="Move wallet-owned credits." copy="The contract derives the sender from the connected wallet and updates both balances atomically." steps={["Connect the wallet holding finalized credits.","Check the recipient address and amount.","Sign once, then verify both wallet balances."]}/>}
