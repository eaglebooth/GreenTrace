import{PageHead}from"@/components/PageHead";import{Reveal}from"@/components/Reveal";const stages=[["SOURCE REGISTRY","The deployment admin approves exact independent evidence hostnames."],["COMPANY OWNER","The signing wallet registers a facility and becomes its immutable owner."],["PENDING_AUDIT","One report per company period binds self, satellite, and sensor evidence."],["AUDIT_PROPOSED","Semantic consensus produces scores; contract code derives decision and credit amount."],["CHALLENGE_PENDING","The company or source admin may add trusted new evidence before issuance."],["FINALIZED","The source admin issues credits once to the authenticated company wallet."],["TRANSFER / RETIRE","Wallet owners can move credits or permanently remove them from circulation."]];export default function P(){return <><PageHead eyebrow="Complete lifecycle" title="Evidence before issuance." copy="GreenTrace separates source governance, company actions, AI judgment, independent finalization, ownership, and retirement."/><section className="section"><div className="work-grid"><Reveal className="guide"><h3>Consensus rule</h3><p>The semantic comparison must agree on the deterministic decision band. The contract then reapplies thresholds and calculates credits itself.</p></Reveal><div className="record-list">{stages.map(([s,d],i)=><Reveal className="record-row" delay={i*.04} key={s}><strong>{String(i+1).padStart(2,"0")}</strong><div><strong>{s}</strong><small>{d}</small></div><span/><span/><span/></Reveal>)}</div></div></section></>}
