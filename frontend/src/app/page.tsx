import Link from "next/link";
import { ArrowRight, BadgeCheck, Factory, RadioTower, Satellite, Sprout } from "lucide-react";
import { Reveal } from "@/components/Reveal";

const steps=[[Factory,"Register verified owner","The connected wallet becomes the company owner. No wallet can be typed or impersonated."],[Satellite,"Bind trusted evidence","Company reports stay on the facility domain; satellite and sensor hosts must be approved."],[RadioTower,"Reach semantic consensus","Validators compare decision bands and source-grounded facts instead of exact prose."],[BadgeCheck,"Finalize and retire","An independent admin finalizes issuance; credits can move between wallets or be retired."]];

export default function Home(){return <><section className="hero"><Reveal><span className="badge"><Sprout size={16}/> GenLayer-native carbon audits</span><h1>Carbon credits rooted in <span>real evidence.</span></h1><p>GreenTrace cross-checks company claims against trusted satellite and public sensor sources, then issues wallet-owned credits through a reviewable on-chain lifecycle.</p><div className="hero-actions"><Link className="pill-button" href="/companies/register">Register facility <ArrowRight size={18}/></Link><Link className="ghost-button" href="/how-it-works">Follow the lifecycle</Link></div></Reveal><Reveal className="canopy"><div className="river-card"><div className="river-row"><span className="brand-mark"><RadioTower size={24}/></span><div><strong>Independent evidence first</strong><span>Authenticated owner / trusted sources / deterministic issuance</span></div><strong>V2</strong></div><div className="meter"><div/></div></div></Reveal></section><section className="section"><div className="section-heading"><h2>Trace every credit.</h2><p>Each stage has one purpose, one contract action, and an on-chain record reviewers can inspect.</p></div><div className="audit-lane">{steps.map(([Icon,title,text],i)=>{const C=Icon as typeof Factory;return <Reveal className="lane-step" delay={i*.05} key={String(title)}><C size={32}/><h3>{String(title)}</h3><p>{String(text)}</p></Reveal>})}</div></section><section className="final-band"><div className="final-inner"><div><h2>Less greenwashing. More accountable climate finance.</h2><p>No production fixtures, no caller-supplied owner, and no transferable credit before independent finalization.</p></div><Link className="pill-button" href="/reports">Inspect reports <ArrowRight size={18}/></Link></div></section></>}
