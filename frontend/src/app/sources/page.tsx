import{WorkflowPage}from"@/components/WorkflowPage";export default function P(){return <WorkflowPage mode="source" eyebrow="Function 01 / add_trusted_source_host" title="Govern independent evidence sources." copy="The deployment admin approves exact hostnames before they can influence carbon issuance." steps={["Use the source-admin wallet created at deployment.","Enter only the exact hostname without a path.","Prefer authoritative satellite, sensor, regulator, or research sources."]}/>}
