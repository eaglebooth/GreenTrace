import{WorkflowPage}from"@/components/WorkflowPage";export default function P(){return <WorkflowPage mode="report" eyebrow="Function 03 / submit_report" title="Assemble a source-bound evidence packet." copy="Only the registered company owner can submit, and requested credits cannot exceed the baseline." steps={["Confirm the reporting period has not been used.","Keep the self-report on the registered facility hostname.","Use approved satellite and sensor hostnames."]}/>}
