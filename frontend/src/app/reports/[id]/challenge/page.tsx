import{WorkflowPage}from"@/components/WorkflowPage";export default async function P({params}:{params:Promise<{id:string}>}){const{id}=await params;return <WorkflowPage mode="challenge" reportId={id} eyebrow="Function 05 / challenge_report" title="Challenge with trusted new evidence." copy="Only the company owner or source admin may open the single pre-issuance challenge." steps={["Confirm the audit is still proposed and not finalized.","Use an approved source hostname.","Sign from an authorized wallet."]}/>}
