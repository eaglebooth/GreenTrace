import{WorkflowPage}from"@/components/WorkflowPage";export default async function P({params}:{params:Promise<{id:string}>}){const{id}=await params;return <WorkflowPage mode="challenge-audit" reportId={id} eyebrow="Function 06 / audit_challenge" title="Reconsider the proposal." copy="The challenge jury compares trusted new evidence with the original audit before updating the proposal." steps={["Confirm status CHALLENGE_PENDING.","Run the second semantic review.","Inspect the new deterministic decision and credit amount."]}/>}
