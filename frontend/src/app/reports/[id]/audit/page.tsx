import{WorkflowPage}from"@/components/WorkflowPage";export default async function P({params}:{params:Promise<{id:string}>}){const{id}=await params;return <WorkflowPage mode="audit" reportId={id} eyebrow="Function 04 / audit_report" title="Run the first carbon jury." copy="GenLayer reads all four sources and reaches semantic consensus before deterministic thresholds calculate proposed credits." steps={["Confirm the report is PENDING_AUDIT.","Check every evidence URL is publicly readable.","Run consensus and sync the resulting proposal."]}/>}
