import{WorkflowPage}from"@/components/WorkflowPage";export default async function P({params}:{params:Promise<{id:string}>}){const{id}=await params;return <WorkflowPage mode="finalize" reportId={id} eyebrow="Function 07 / finalize_issuance" title="Issue credits exactly once." copy="Only the source admin can finalize a proposal and credit the authenticated company owner's wallet." steps={["Confirm no challenge remains pending.","Review decision, scores, reduction, and proposed amount.","Finalize once and sync wallet ownership."]}/>}
